server:
  port: 8080
hm:
  jwt:
    location: classpath:hmall.jks
    alias: hmall
    password: hmall123
    tokenTTL: 1d
    access_tokenTTL: 30m
    refresh_tokenTTL: 1d
  auth:
    excludePaths:
      - /search/**
      - /users/login
      - /items/**
      - /hi
